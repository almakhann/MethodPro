#Decision Tree
#Regression(Linear,Logistic, Mixed, Generalized)
#Random Forest (regression, calssification)
#
#Time-Series
#  Prophet
#
#xgboost
#
#Flexibili




library(crypto) # Coinmarketcup
library(ggplot2)
df <- crypto_history(coin="bitcoin")
df <- df[,c(4,9)]
str(df)
library(lubridate) # Handle date
head(df)
df$date <- ymd(df$date)

library(prophet) # Time Series model.
#   ?????? ???????????? ?? prophet, ???????????????????? ?????? ??????????????
#   ds - data, y - ????????????????????
#   prophet   ???????????? ???????????????? ?? ????????????????
colnames(df) <- c("ds","y")
?prophet
m <- prophet(df)
future <- make_future_dataframe(m,periods = 60)
forecast <- predict(m,future)
plot(m,forecast)
prophet_plot_components(m,forecast)

f <- select(forecast, c(ds,yhat))
f %>% top_n(60,ds)

